name: e2e

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  playwright:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
      - uses: astral-sh/setup-uv@v5
      - name: Native build dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config libx11-dev libxkbfile-dev libsecret-1-dev
      - name: Prepare workspace
        run: |
          mkdir -p "${{ runner.temp }}/arc-workspace"
          echo "ARC_WORKSPACE_PATH=${{ runner.temp }}/arc-workspace" >> "$GITHUB_ENV"
      - run: pnpm install --frozen-lockfile
      - name: Sync Python dependencies
        working-directory: python
        run: uv sync --all-extras --dev
      - name: Add ARC Python CLI to PATH
        run: echo "${{ github.workspace }}/python/.venv/bin" >> "$GITHUB_PATH"
      - name: Ensure e2e stub is executable
        run: chmod +x tests/e2e/fixtures/swarmgraph-stub.sh
      - run: pnpm --filter @arc-studio/e2e-tests exec playwright install chromium
      - run: pnpm build
      - run: pnpm test:e2e
        env:
          ARC_SWARMGRAPH_RUN_BACKEND: stub
      - name: Upload Playwright artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts
          path: |
            test-results/
            tests/e2e/test-results/
          if-no-files-found: ignore
