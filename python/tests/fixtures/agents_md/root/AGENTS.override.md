# AGENTS.override.md — arc-theia-studio

This override file takes priority over the base AGENTS.md in this directory.

## Override: Strict Mode

When this override is active:
- All destructive operations require explicit HITL approval
- Network access is completely disabled
- Only read-only sandbox policy is permitted

## arc-theia-studio Context

This workspace override enforces maximum safety for automated agents.
