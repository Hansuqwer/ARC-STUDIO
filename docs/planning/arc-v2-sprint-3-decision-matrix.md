# ARC v2 — Sprint-3 Framework Decision Matrix (template — fill from spike reports)

Status: **TEMPLATE** — numbers come only from committed `reports/spike-*.json`
files generated by spike-harness on the spike machine. Hand-entered numbers are
not acceptable evidence.

Machine: _________________ (must match report `machine` block) · Display: ____ Hz
Pinned benchmark machine: yes / no (if no: matrix is indicative-only, no selection may be made)

## Automatic gates

| Gate (bar) | gpui @ ____ | gpui-ce @ ____ | floem @ ____ | bespoke @ ____ |
|---|---|---|---|---|
| G1 first paint 100 MB (≤1000 ms) | | | | |
| G1 pathological single-line (≤1000 ms) | | | | |
| G2 diff scroll p99 (≤1.0 frame) | | | | |
| G3 replay 100 rows (≤250 ms total, no frame >33 ms) — headless baseline: p50 0.12 ms | | | | |
| G4 typing p99 (≤1.0 frame) | | | | |

## Evidence gates (artifact paths, not adjectives)

| Gate | gpui | gpui-ce | floem | bespoke |
|---|---|---|---|---|
| G5 VoiceOver / Orca(X11+Wayland) / NVDA | | | | |
| G6 IME matrix (JA/ZH/KO × macOS/Linux-fcitx5/Linux-ibus/Windows + dead keys) | | | | |
| G7 bidi+ligature golden | | | | |
| G8 cadence / bus factor / breakage rate / vendoring cost | | | | |

## Decision rule (ADR-0002 addendum — owner-confirmed at ARC2-13 before use)

Among candidates whose `spike_verdict()` is clean:
1. **Facade cost** — diff size + concept mismatch of implementing `arc_ui::kit`
   (estimate by porting the Sprint-2 ShellModel render: palette, focus ring,
   status rail). Smallest wins.
2. **G8 sustainability** — tie-break 1. A candidate with "unbounded" vendoring
   cost cannot win a tie.
3. **A11y/IME evidence quality** — tie-break 2.

GPUI holds no default preference (supersedes ADR-0002's "both pass → GPUI").
If no candidate is clean → bespoke gets one contained sprint (Masonry permitted
as widget layer). If bespoke fails → escalate to owner. Never silently select
WebView/Tauri/Electron.

## Verdict

Selected: _________ · Owner sign-off: _________ · Date: _________
Follow-ups (facade feature flag, window-mode arc-shell, G-gate re-runs in CI): _________
