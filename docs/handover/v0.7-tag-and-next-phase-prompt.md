# v0.7-alpha Tag + Next-Phase Prompt

---

## PART 1 — Tag v0.7-alpha (paste-ready)

```text
Tag v0.7-alpha. Branch spec/v0.7-opt-in-cloud-features is green and reviewed.

Pre-tag verification (run, paste output):
  cd /Users/hansvilund/HansuQWER/WorkSpace/ARC/arc-theia-studio/python
  uv run pytest -q --ignore=tests/e2e --ignore=tests/integration 2>&1 | tail -3
  # expect: 5131 passed, 30 skipped, 5 xfailed
  cd ../packages/arc-protocol-ts && pnpm build && pnpm test 2>&1 | tail -3
  # expect: 155 passed

Merge + tag + push:
  cd /Users/hansvilund/HansuQWER/WorkSpace/ARC/arc-theia-studio
  git checkout main && git pull --ff-only
  git merge --no-ff spec/v0.7-opt-in-cloud-features -m "feat: v0.7-alpha — opt-in cloud features

Three independent opt-in features, all default OFF:
1. Pricing feed (hash-pinned, OpenRouter primary / models.dev fallback)
2. Shared budget broker (team mode, fail-closed)
3. Observability bridge (per-session consent, sanitized semantic metrics only)

3 typed events mirrored Python + TS. Threat model documented.
5091 → 5131 tests (+40). TS 149 → 155 (+6).
Inherits 86043fe + 1aa2da5 from main (see MERGE_NOTES inheritance section)."

  git tag -a v0.7-alpha -m "Opt-in cloud features.
Pricing feed (hash-pinning), budget broker (team mode), observability bridge
(consent-gated, sanitized). All default OFF, fail-closed, local-first preserved.
+40 Python tests, +6 TS. Threat model: docs/threat-models/v0.7-opt-in.md."

  git push origin main v0.7-alpha

Post-tag:
  - Watch the 5 workflows; report the grid.
  - Write docs/research/v0.7-opt-in-cloud-postmortem.md (predicted vs actual:
    3 features / +40 tests / hash-pinning-not-Ed25519 correction / smokes deferred).
  - Report tag SHA + workflow grid.
```

---

## PART 2 — Next phase (planning, not execution)

**Context for whoever picks this up:** v0.7-alpha is the last pre-written spec.
The token-saving series (R-TS1 → R-TS7) is complete:

| Tag | Theme |
|-----|-------|
| v0.3.0–0.3.1 | Token-saving P0 + CI debt |
| v0.4.0–0.4.1 | TokenWallet + budget persistence + pricing |
| v0.5.0 | R-02 compaction + QW-4 handles |
| v0.5.1–0.5.2 | Chinese-lab vendors + capability fields |
| v0.6 | Catalog-driven model picker |
| v0.7 | Opt-in cloud features |

There is **no queued spec after v0.7**. The next phase is a **planning sprint**,
not an execution sprint. Do NOT invent an implementation and start coding.

### Next-phase prompt (paste-ready)

```text
v0.7-alpha is tagged. The token-saving + cloud series is complete and there is
no queued spec. Run a PLANNING phase, not an execution phase.

Step 1 — Reality audit (~30 min, no code changes):
  - Read docs/roadmap.md end to end. Which rows are still "Planned" or "Blocked"?
  - Read docs/REALITY_AUDIT.md if present — what did it flag as incomplete?
  - grep for "TODO", "FIXME", "alpha-only", "stub", "not yet" across
    python/src/agent_runtime_cockpit/ — categorize the top 10 by impact.
  - List the pre-existing test failures + 5 xfails. Are any now fixable?

Step 2 — Candidate next-sprint options (write docs/research/post-v0.7-options.md):
  Propose 3-4 candidate sprints with effort + risk + dependency for each.
  Likely candidates given current state:
    A. Hardening: convert the 5 xfails + 3 env-flaky tests to green
       (deterministic snapshot IDs, SQLite lock retry, auth fallback fix).
    B. The microVM / sandbox phase from the original AGENTS.md mandate
       (docs/research/sandbox-and-microvm.md exists; Lima/Firecracker
       preflight → real execution). This was the ORIGINAL primary goal.
    C. Anthropic in VENDOR_CONFIGS: /models --vendor anthropic still thin;
       unify the Anthropic provider into the catalog so the picker is complete.
    D. Runtime feed activation: v0.7 shipped the opt-in plumbing; a follow-up
       could deploy a real signed/pinned feed + the `arc pricing-feed` CLI
       subcommand group (currently the accept-new-hash is a stub).

  For each: spec-readiness (is there a doc?), blast radius, and whether it
  needs new research (Context7/Vercel grep/web) before a spec can be written.

Step 3 — Recommend ONE, with rationale. Do not start it.
  Surface the recommendation + the options doc. Await user pick.

Constraints (unchanged):
  - No code changes during planning phase except the options doc.
  - File:line citations for every "X is incomplete" claim.
  - honesty-over-polish: if something claimed-done is actually a stub, say so.

Branch: none yet. Planning output is a single docs/research/ commit on main
(or a docs branch), no feature branch until a sprint is picked.
```

### My read on the strongest candidate

**Option B (microVM/sandbox)** is the original primary mandate from `AGENTS.md`
("Turn ARC Studio's existing trust/isolation/security foundation into a
production-grade CLI sandbox foundation, with a path to lightweight microVM
execution"). The token-saving series was a detour — valuable, shipped, done.
The sandbox work is the headline feature that's still preflight-only.

But it's the highest-risk and needs the most research first. If the goal is
momentum, **Option A (hardening)** is a clean 1-day win that makes the test
suite fully deterministic before tackling something big.

Recommend the planning phase decide between A (momentum) and B (mandate).
```
