{
  "name": "@arc/mobile-expo",
  "version": "0.1.0-alpha",
  "private": true,
  "description": "ARC Mobile Runtime Expo stub — simulator only (not publishable without build step)",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "source": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
