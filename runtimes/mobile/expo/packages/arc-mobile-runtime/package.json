{
  "name": "arc-mobile-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "ARC Mobile Runtime — Expo simulator stub (preview, mock-native only, not publishable without build)",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "source": "src/index.ts",
  "exports": {
    ".": {
      "types": "./build/index.d.ts",
      "default": "./build/index.js"
    }
  },
  "scripts": {
    "build": "expo-module build",
    "clean": "expo-module clean",
    "lint": "expo-module lint",
    "test": "jest --passWithNoTests"
  },
  "keywords": ["arc", "mobile", "runtime", "expo", "agent", "stub", "simulator-preview"],
  "license": "Apache-2.0",
  "peerDependencies": {
    "expo": ">=50"
  },
  "devDependencies": {
    "expo-module-scripts": "^3.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
