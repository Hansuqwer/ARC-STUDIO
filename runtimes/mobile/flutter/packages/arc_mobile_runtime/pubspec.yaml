name: arc_mobile_runtime
description: "ARC Mobile Runtime — Flutter simulator stub (preview; pubspec only, zero Dart source, no platform plugin, not publishable to pub.dev)."
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
