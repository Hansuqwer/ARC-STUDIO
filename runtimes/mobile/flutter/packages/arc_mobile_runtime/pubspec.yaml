name: arc_mobile_runtime
description: ARC Mobile Runtime SDK — Flutter stub (MVP, types only, no real native bridges).
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
