{
  "name": "@arc-studio/protocol",
  "version": "0.1.0-alpha",
  "description": "ARC protocol TypeScript types — shared between extensions and packages",
  "license": "Apache-2.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf lib",
    "test": "node -e \"const p = require('./lib/index'); console.log('protocol types ok:', typeof p.ARC_PROTOCOL_VERSION)\"",
    "check": "echo '@arc-studio/protocol check ok'"
  },
  "devDependencies": {
    "rimraf": "^5.0.10",
    "typescript": "^5.4.5"
  }
}
