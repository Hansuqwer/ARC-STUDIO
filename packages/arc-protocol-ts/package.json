{
  "name": "@arc-studio/protocol",
  "version": "0.2.0-alpha",
  "description": "ARC protocol TypeScript types — shared between extensions and packages",
  "license": "Apache-2.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf lib",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "check": "echo '@arc-studio/protocol check ok'"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "rimraf": "^5.0.10",
    "ts-jest": "^29.0.0",
    "typescript": "^5.4.5"
  }
}
